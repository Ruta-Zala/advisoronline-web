query PartnerCollections {
	playCollections(sort: "publishedAt:desc") {
		data {
			id
			attributes {
				name
				slug
				description
				header {
					data {
						id
						attributes {
							url
						}
					}
				}
			}
		}
	}
}
